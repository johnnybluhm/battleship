package edu.colorado.exhalation;
  
public class Main {

    public static void main(String[] args) {
        // write your code here
        //change
         //Ship ship = new Ship();
         //ship.show();
        /* Example of how main class might be written with Command pattern
        Game battleShip = new Game();
        Board temp = new Board();
        battleShip.setBoard(temp);

        PlayerAction playerActions = new PlayerAction();
        playerActions.SetCommand(0, new MoveShips(battleShip));
        playerActions.SetCommand(1, new Hit(battleShip));

        playerActions.Move(0, 'E');
        playerActions.Undo();
        playerActions.Redo();
        playerActions.Action(1, new Point(1, 1));
        */
    }
}
